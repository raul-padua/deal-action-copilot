# Dev-only: browser talks to FastAPI directly so SSE streams without the
# Next.js dev proxy buffering it. In production (Vercel) this is unset and
# the frontend uses same-origin /api/py/* routes.
NEXT_PUBLIC_API_BASE=http://localhost:8000
